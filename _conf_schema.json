{
    "group_whitelist": {
        "description": "群聊白名单",
        "hint": "仅在白名单里的群启用本插件功能，不填表示所有群都启用",
        "type": "list",
        "default": []
    },
    "notify_on_success": {
        "description": "成功时发送通知",
        "hint": "开启后，文件检查有效时也会发送一条回复通知。关闭则只在文件失效时通知。",
        "type": "bool",
        "default": true
    },
    "pre_check_delay_seconds": {
        "description": "预检延时 (秒)",
        "hint": "收到文件后，等待多少秒再开始进行阶段一的即时检查。建议设为3-10秒。",
        "type": "int",
        "default": 10
    },
    "check_delay_seconds": {
        "description": "复核间隔时间 (秒)",
        "hint": "预检通过后，再等待多少秒进行时效性复核。",
        "type": "int",
        "default": 300
    },
    "preview_length": {
        "description": "文本预览长度 (字符)",
        "hint": "设置在回复中显示的文本内容预览的最大字符数。",
        "type": "int",
        "default": 200
    },
    "forward_threshold": {
        "description": "长消息合并转发阈值 (字符)",
        "hint": "当插件的单条回复（主要是带预览的）超过此字数时，将自动转为合并转发。设置为 0 则禁用此功能。",
        "type": "int",
        "default": 300
    },
    "enable_duplicate_check": {
        "description": "启用文件重复性检查",
        "hint": "开启后，当新文件上传时，将根据文件大小检查群文件中是否已存在相同文件。",
        "type": "bool",
        "default": false
    },
    "enable_zip_preview": {
        "description": "启用ZIP文件预览",
        "hint": "开启后，插件会尝试解压收到的ZIP压缩包，并对其中的TXT文件进行内容预览。",
        "type": "bool",
        "default": true
    },
    "default_zip_password": {
        "description": "ZIP文件默认解压密码",
        "hint": "当直接解压ZIP失败时，会尝试使用此密码进行解压。留空表示不使用密码重试。",
        "type": "string",
        "default": ""
    },
    "enable_repack_on_failure": {
        "description": "为失效TXT重新打包发送",
        "hint": "开启后，当TXT文件初步检查失效但机器人仍能下载时，会自动将其压缩为ZIP文件尝试发送。",
        "type": "bool",
        "default": false
    },
    "repack_zip_password": {
        "description": "重新打包ZIP的加密密码",
        "hint": "为失效TXT重新打包成ZIP文件时使用的加密密码。留空表示不加密。",
        "type": "string",
        "default": ""
    }
}
